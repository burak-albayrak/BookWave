import React from 'react';

const Header = ({ username }) => {
    return (
        <header>
            <h1>Welcome </h1>
            <button>Profile</button>
        </header>
    );
};

export default Header;
